/* ---------------------------------------------------------------
   Paste your Firebase web config between the quotes below and the
   app starts syncing. Leave it empty and everything keeps working
   exactly as before, saved only in this browser.

   Where to find these: Firebase console → your project →
   Project settings → General → "Your apps" → Web app → SDK setup.

   These values are meant to be public — they identify the project,
   they don't grant access. Access is decided by the database rules
   (see README.md), which only let your two Google accounts in.
   --------------------------------------------------------------- */

window.FIREBASE_CONFIG = {
  apiKey: "AIzaSyDPOTT94xlpYhvnkazEk_C209aSuvYglWs",
  authDomain: "cute-date106.firebaseapp.com",
  // Realtime Database → Data tab: the URL printed above the data tree.
  // Syncing stays off until this is filled in.
  databaseURL: "",
  projectId: "cute-date106",
  appId: "1:263162280493:web:53d5b59c9c3a0f928156a4"
};

/* Both of you must use the same space name — it's the shared calendar. */
window.DATE_SPACE = "us";
